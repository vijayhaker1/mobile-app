// What is Modal component?
/*
  The `Modal` component is a React-Native's core component that is used to show modal screen or popup window in React-Native that overlays the app content to provide important information or, prompt the user for a decision to make.

  Since the `Modal` component is purposefully interruptive we should ensure to use it only when necessary.

  The `Modal` component is a container or block-level component which means it do have a closing tag and we can define modal content is the `Modal` component or, inside the `Modal` we can define `View` component to show the desire content to be presented inside the modal.


  `Modal` component by default it always set to visible to show modal based on the user interation we have to pass the props, the props that `Modal` accepts:  
*/